// fn mcts(root: &mut InformationSet, iterations: u32) -> Move {
//     for _ in 0..iterations {
//         let determinized_state = root.known_game_state.determinize();
//         // Proceed with MCTS simulation, expansion, and backpropagation
//     }
//     // Select the best move from the root information set
// }
